header.bg-indigo-600.p-4
    div.container.mx-auto.flex.justify-between.items-center
        a(href='/')
            h1.text-2xl.text-white.font-extrabold.text-center Comparador


        nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white")
          a(href="/auth/registro") Crear Cuenta
          a(href="/auth/login") Iniciar Sesion

div(class="bg-indigo-700 py-5 hidden lg:block")
    div(class="container mx-auto flex justify-between")
        nav(class="flex gap-4 items-center")
            a(href="#" class="text-sm font-bold uppercase text-white") Promociones
            a(href="/laptops" class="text-sm font-bold uppercase text-white") Computadoras
            a(href="/telefonos" class="text-sm font-bold uppercase text-white") Celulares
            a(href="/tablets" class="text-sm font-bold uppercase text-white") Tablets
        form(action="/buscador" method="POST" class="flex gap-4 items-center")
            input(type="hidden" name="_csrf" value= csrfToken)
            label(for="termino" class="text-sm uppercase font-bold text-white") Busqueda  
            input(type="text" id="termino" name="termino" placeholder="Buscar Productos" class="p-2 rounded-lg shadow text-sm")
            input(type="submit" value="Buscar" class="bg-indigo-800 rounded-lg hover:bg-indigo-400 text-white uppercase font-bold p-2 cursor-pointer text-sm")