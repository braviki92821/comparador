extends ../layout/app

block styles
    meta(name="csrf-token" content=csrfToken)

block contenido
    div.py-10
        h2.text-center.text-2xl.font-extrabold= pagina

        //pre= JSON.stringify(tablets, undefined, 2)
        if tablets.length 
            include ../templates/tablet
        else 
            p.text-center No hay Tablets
        a(href="/compararTablets" class="!fixed hidden bottom-5 right-5  rounded-full bg-red-600 p-3 text-xs font-medium uppercase leading-tight text-white shadow-md transition duration-150 ease-in-out hover:bg-red-700 hover:shadow-lg focus:bg-red-700 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-red-800 active:shadow-lg" id="btn-back-to-top"  ) Ir a comparar

block scripts
    script(src="/js/agregarComparacion.js")
    script(src="/js/navegacion.js") 